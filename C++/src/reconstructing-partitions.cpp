 /**
 * @author Stephen Hilliard (c) 2015.
 * @developer Stephen Hilliard (c) 2015.
 */
 /*Reconstructing Partitions*/
int recoverPartitions(reconstructPartition)
{std::recoverPartitions;
getline (std::(p,V), recoverPartitions);
/*Fragment*/
for (p) {
	Pos = 0; 
	Neg = 0;
	Att = Attrs(p);
	p[T] = p;
	}
	
	for (i=l; i<=n; p[T] > l; (Att) > 0; i++)
		{
		if (p = V) { 
			 (Pos = Pos V; p[T] = p[T] V; Att = Att - Attrs(v))
				}
			}
/*Subtract*/
for (i=n; i >== l; (pT) > l; i--)
{
	if { 
		p = V (Neg = Neg V); 
		return (p[T]= p[T]-V);
		}
	if { 
		p[T] = l; (Att) = 0;
		return (Recovered [p]=(Pos,Neg));
		}
	}
	return Recovered;
}

reconstruct_partition(int s[], int d[MAXN+1][MAXK+1], int n, int k)
{
		if (k==1)
				write_SQL(s,1,n);
		else {
				reconstruct_partition(s,d,d[n][k],k-1);
				write_SQL(s,d[n][k]+1,n);
		}
}

write_SQL(int s[], int start, int end)
{
		int i;										/*counter*/

		for (i=start; i<=end; i++) writef(" %d ",s[i]);
		writef("\n");
}